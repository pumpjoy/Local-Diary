view_custom_property & custom_row_widget
 - property_type
 - changable property_type
 - json reflects this change
 - all visual change
 - directly save to json immediately after changes
 - rename-able property_key
 - Warning when changing property_type
 - Duplicates the property with content when
   changing property_type
   (So that user can manually delete the duplicate when
   they are absolutely sure they don't want the contents
   of this type_property)